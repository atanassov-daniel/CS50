#include <cs50.h> // doesn't work without the cs50 library for some reason
#include <stdio.h>

int main(void)
{
    printf("false == %i\n", false); // false == 0
    printf("true == %i\n", true); // true == 1
}